n = int(input())
d = {}

d = dict(input().split() for _ in range(n))
while True:

    try:

        search = input()

        if search in d:

            print(search + '=' + d[search])
    
        else:

            print('Not found')
                        
    except:

        break
